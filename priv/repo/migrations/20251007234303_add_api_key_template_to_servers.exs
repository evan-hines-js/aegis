defmodule Aegis.Repo.Migrations.AddApiKeyTemplateToServers do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:servers) do
      add :api_key_template, :text, default: "Bearer {API_KEY}"
    end
  end

  def down do
    alter table(:servers) do
      remove :api_key_template
    end
  end
end
